// examples/demo.ltl — runnable demo for ltl_epoll_demo

import std::io
use ltl_epoll_demo as lib

fn main() {
    io::println("ltl_epoll_demo demo starting...")
    let result = lib::main() |> io::inspect()
    io::println("demo complete")
}