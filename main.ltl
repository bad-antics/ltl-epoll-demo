// ltl-epoll-demo/main.ltl
//
// epoll-based reactor — accept / read / write fan-out.
//
// This is an early skeleton — contribution is welcome! See the
// README for build instructions.

import std::io
import std::fmt

pub fn main(args: [str]) -> Int {
    io::println("ltl-epoll-demo v0.1.0")
    io::println("epoll-based reactor — accept / read / write fan-out.")
    0
}
